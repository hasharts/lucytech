
version: "3.9"

services:
  backend:
    build: ./backend
    container_name: web-analyzer-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: web-analyzer-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
